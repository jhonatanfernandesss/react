name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    
      - run: echo "🎉 A tarefa foi disparada automaticamente por um evento $ {{github.event_name}}."
      - run: echo "🐧 Este trabalho agora está sendo executado em um servidor $ {{runner.os}} hospedado pelo GitHub!"
      - run: echo "🔎 O nome do seu branch é $ {{github.ref}} e seu repositório é $ {{github.repository}}."
      - nome: Verifique o código do repositório
        usa: ações / checkout @ v2
      - run: echo "💡 O repositório $ {{github.repository}} foi clonado para o runner."
      - run: echo "🖥️ O fluxo de trabalho agora está pronto para testar seu código no executor."
      - nome: lista os arquivos no repositório
        correr: |
          ls $ {{github.workspace}}
      - run: echo "🍏 O status deste trabalho é $ {{job.status}}."
